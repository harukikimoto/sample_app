詳細です

<h2>
    <% if @user.id == @current_user.id %>
    <img src="<%= "/user_images/#{@user.image_name}" %>">
    <%= link_to(@user.name, "/users/#{@user.id}/edit") %>
    <% elsif %>
        <%= @user.name %>
    <% end %> 
</h2>
<p><%= @user.email %></p>